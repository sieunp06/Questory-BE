<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.questory.trip.repository.TripPermissionRepository">
    <select id="isTripParticipant" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM trip t
            JOIN party_member pm ON pm.party_id = t.party_id
            WHERE t.trip_id = #{tripId}
                AND pm.member_id = #{memberId}
        )
    </select>
</mapper>