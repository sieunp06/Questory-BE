<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.questory.member.repository.MemberRepository">
    <select id="findById" parameterType="long" resultType="com.ssafy.questory.member.domain.Member">
        SELECT member_id, email, nickname, status
        FROM member
        WHERE member_id = #{memberId}
    </select>
    <select id="findByEmail" resultType="com.ssafy.questory.member.domain.Member">
        SELECT member_id, email, nickname, status
        FROM member
        WHERE email = #{email}
    </select>
    <select id="findStatusById" resultType="com.ssafy.questory.member.domain.MemberStatus">
        SELECT status
        FROM member
        WHERE member_id = #{memberId}
    </select>
    <select id="findLoginPrincipalByEmailWithPassword"
            resultType="com.ssafy.questory.member.dto.security.LoginPrincipalRow">
        SELECT
        m.member_id            AS memberId,
        m.email                AS email,
        m.nickname             AS nickname,
        m.status               AS status,
        p.password_hash        AS passwordHash,
        p.failed_login_count   AS failedLoginCount,
        p.locked_until         AS lockedUntil
        FROM member m
        JOIN member_password_credentials p
        ON m.member_id = p.member_id
        WHERE m.email = #{email}
        AND m.status != 'SOFT_DELETE'
    </select>
    <select id="findMyInfo" parameterType="long"
            resultType="com.ssafy.questory.member.dto.response.MemberInfoResponseDto">
        SELECT m.email, m.nickname, m.total_exp AS total_exp, t.name AS representative_title
        FROM member m
        LEFT JOIN title t ON m.representative_title_id = t.title_id
        WHERE m.member_id = #{memberId}
            AND m.status != 'SOFT_DELETE'
    </select>

    <select id="searchByEmail" resultType="com.ssafy.questory.member.dto.response.MemberInfoResponseDto">
        SELECT m.email, m.nickname, m.total_exp AS total_exp, t.name AS representative_title
        FROM member m
        LEFT JOIN title t ON m.representative_title_id = t.title_id
        WHERE m.email LIKE CONCAT('%', #{email}, '%')
            AND m.member_id != #{myMemberId}
            AND m.status != 'SOFT_DELETE'
        ORDER BY m.email
    </select>

    <insert id="register"
            parameterType="com.ssafy.questory.member.domain.Member"
            useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO member (email, nickname, status, created_at, updated_at)
        VALUES (#{email}, #{nickname}, #{status}, NOW(), NOW())
    </insert>
    <update id="softDeleteIfActive">
        UPDATE member
        SET status = 'SOFT_DELETE', updated_at = #{now}
        WHERE member_id = #{memberId}
            AND status = 'NORMAL'
    </update>

    <update id="changeStatusLocked">
        UPDATE member
        SET status = 'LOCKED'
        WHERE email = #{email}
    </update>
    <update id="changeStatusNormal">
        UPDATE member
        SET status = 'NORMAL'
        WHERE email = #{email}
    </update>

    <update id="updateRepresentativeTitle">
        UPDATE member
        SET representative_title_id = #{titleId}
        WHERE member_id = #{memberId}
    </update>
</mapper>